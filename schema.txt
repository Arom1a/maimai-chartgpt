bpm list,
note {
    timestamp, pos, note_deco, duration, slide_deco, segments
}

---

bpm list: {
    [(timestamp, bpm_change)]
}
timestamp: f32
pos: 1-8, 9-42 (for A1-8, B, ..., C, D, E for touches)
note_deco: {
    break, ex, pseudo_each, firework
}
duration: f32
slide_deco: {
    break
}
segments: {
    end_pos: 1-8,
    shape: {
        -
        v
        <> (convert ^ to corresponding <>)
        sz (diagonal only)
        pqppqq
        w
        V
    }
}

---

note_types:

TAP:
timestamp
pos
note_deco

HOLD:
timestamp
pos
note_deco
duration

SLIDE:
timestamp (slide head time)
pos (slide start pos)
note_deco (slide head deco)
duration (trace duration, not counting wating beat)
slide_deco (trace deco)
segments (list of (shape, end_pos) for each segment)
<!-- fade (`?`, `!`) (omitted as not present in my dataset) -->
<!-- star_visible (`@`, `$`) (omitted as not present in my dataset) -->

TOUCH:
timestamp
pos
note_deco
duration

TOUCHHOLD:
timestamp
pos
note_deco
duration

---

use <NA> for "not apply" tokens
